[package]
name = "infiltrator-contract"
version = "0.30.0"
edition = "2024"
publish = false
description = "Stable cross-surface commands, snapshots, events, capabilities, and failures."

[dependencies]
anyhow = { workspace = true }
serde = { workspace = true }
